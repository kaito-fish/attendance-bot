name = "attendance-bot"
main = "worker/index.js"
compatibility_date = "2026-06-23"

[observability]
enabled = true

# このブロックは scripts/generate-crons.mjs が worker/config.js から自動生成する。手で編集しない。
# `npm run generate:crons` で更新される (npm run deploy 時にも自動実行)。
[triggers]
crons = []
